[project]
name = "ava"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "claude-code-sdk>=0.0.14",
    "boto3>=1.35.0",
    "PyGithub>=2.3.0",
    "loguru>=0.7.2",
    "python-dotenv>=1.0.0",
    "anyio>=4.4.0",
]

[project.scripts]
ava = "main:main"

[tool.uv]
