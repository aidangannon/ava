meta {
    name: Send Message
}

post {
    url: {{base_url}}/messages
    body: json
    auth: none
}

body:json {
    {
        "message": "heello"
    }
}

script:post-response {
  console.log(res.getBody());
}
